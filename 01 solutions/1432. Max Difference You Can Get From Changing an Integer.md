---
difficulty: medium
tags:
  - dsa/problem
  - dsa/topics/greedy
status: solved
created: 2025-06-15 14:54
link: https://leetcode.com/problems/max-difference-you-can-get-from-changing-an-integer/
lastReviewed: 2026-05-19
reviewCount: 6
---
# 1432. Max Difference You Can Get From Changing an Integer

You are given an integer `num`. You will apply the following steps to `num` **two** separate times:

- Pick a digit `x (0 <= x <= 9)`.
- Pick another digit `y (0 <= y <= 9)`. Note `y` can be equal to `x`.
- Replace all the occurrences of `x` in the decimal representation of `num` by `y`.

Let `a` and `b` be the two results from applying the operation to `num` *independently*.

Return *the max difference* between `a` and `b`.

Note that neither `a` nor `b` may have any leading zeros, and **must not** be 0.

**Example 1:**

```
Input: num = 555
Output: 888
Explanation: The first time pick x = 5 and y = 9 and store the new integer in a.
The second time pick x = 5 and y = 1 and store the new integer in b.
We have now a = 999 and b = 111 and max difference = 888
```

**Example 2:**

```
Input: num = 9
Output: 8
Explanation: The first time pick x = 9 and y = 9 and store the new integer in a.
The second time pick x = 9 and y = 1 and store the new integer in b.
We have now a = 9 and b = 1 and max difference = 8
```

**Constraints:**

- 1 <= num <= 10<sup>8</sup>


```ad-tip
title: hint 1
collapse:close
We need to get the max and min value after changing num and the answer is max - min.

```

```ad-hint
title: hint 2
collapse: close
Use brute force, try all possible changes and keep the minimum and maximum values.
```

# Mynote

1. pick 1 chữ số. Xử lý với các chữ số đó trong số ban đầu
2. Thay bằng chữ số khác sao cho
	1. thu được max
	2. thu được min
3. Tìm max diff

notes:
- Không 0 leading
- có thể pick số để số gốc k đổi

---

## Greedy

**Approach:**

1. Tìm max
	1. Dạng 9xxx
	2. Tìm digit != 9 -> thay bằng 9
2. Tìm min, 2 case
	1. case1: đầu 1 dạng 1xxxx
		1. đổi x != 1 và ==x != 0== -> đổi x thành 0
	2. case 2: đầu khác 1 dạng axxx
		1. đổi a về 1

```ad-fail
tittle: chú ý x ở case 1 tìm min
x phải khác 0 nữa để dồn xử lý các hàng đơn vị sau nếu đụng phải 0 sớm
ví dụ: 1101052 -> 1101002 (5 -> 0) thay vì ~~1101052 (giữ nguyên 0 và k đỏi 5)~~
```

```ad-tip
title: testcase
555
9
12356
111
1101057
99259
```


```cpp
class Solution {
public:
    int maxDiff(int num) {
        string s = to_string(num);
        string maxStr = s, minStr = s;
        
        // Maximize: Replace first non-9 with 9
        for (int i = 0; i < s.size(); i++) {
            if (maxStr[i] != '9') {
                char target = maxStr[i];
                for (char& c : maxStr) {
                    if (c == target) c = '9';
                }
                break;
            }
        }
        
        // Minimize: Two-case strategy
        if (minStr[0] != '1') {
            // Case 1: Replace first digit with 1
            char target = minStr[0];
            for (char& c : minStr) {
                if (c == target) c = '1';
            }
        } else {
            // Case 2: Find first non-0, non-1 digit and replace with 0
            for (int i = 1; i < minStr.size(); i++) {
                if (minStr[i] != '0' && minStr[i] != '1') {
                    char target = minStr[i];
                    for (char& c : minStr) {
                        if (c == target) c = '0';
                    }
                    break;
                }
            }
        }
        
        return stoi(maxStr) - stoi(minStr);
    }
};
```

