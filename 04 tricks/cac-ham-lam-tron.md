```ad-note
gọi `#include <cmath>`
xem thêm: [[lam-tron-int-khong-qua-double]]
```
# round()

Làm tròn tới số nguyên gần nhất.
# ceil()

luôn làm tròn lên

```
3.4 -> 4
-3.4 -> -3
```

# floor()

Ngược ceil(), luôn làm tròn xuống

# trunc()

cắt bỏ phần thập phân (giống với ép kiểu int)

